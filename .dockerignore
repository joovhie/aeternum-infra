# Dependencies — reinstalled during Docker build
node_modules
**/node_modules

# Build outputs — rebuilt during Docker build
dist
**/.turbo

# Ponder artifacts — indexer only, not needed by keeper
apps/indexer/.ponder
apps/indexer/generated

# Environment files — secrets are injected by Back4App
.env
**/.env
!**/.env.example

# Test files — not needed in production image
**/test
**/*.test.ts
**/vitest.config.ts
**/coverage

# Git
.git
.gitignore

# Docs
README.md
**/README.md
COMMITS.md