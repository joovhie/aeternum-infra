{
  "name": "@aeternum/indexer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":     "dotenv -e ../../.env.local -e .env.local -- ponder dev",
    "build":   "dotenv -e ../../.env.local -e .env.local -- ponder codegen",
    "start":   "dotenv -e ../../.env.local -e .env.local -- ponder start",
    "start:prod": "NODE_OPTIONS='--max-old-space-size=160' ponder start",
    "codegen": "dotenv -e ../../.env.local -e .env.local -- ponder codegen"
  },
  "dependencies": {
    "@aeternum/blockchain": "workspace:*",
    "hono": "^4.12.23",
    "ponder": "^0.16.6",
    "viem": "^2.51.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
