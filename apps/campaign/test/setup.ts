// test/setup.ts
// Global test setup — runs once before all suites. Currently a no-op;
// mirrors apps/keeper's setup.ts so the two apps stay structurally
// consistent even though campaign has no fixtures requiring setup yet.
export {};
